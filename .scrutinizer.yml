build:
    nodes:
        coverage:
            tests:
                override:
                    -   command: vendor/bin/phpunit -v --exclude expensive --coverage-clover /home/scrutinizer/build/coverage/clover.xml
                        coverage:
                            file: /home/scrutinizer/build/coverage/clover.xml
                            format: clover
            environment:
                php:
                    version: 7.4
